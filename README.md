# A simple PHP CRUD application for managing appointments.

# Requirements:
- PHP 7.0 or higher
- pdo_sqlite extension enabled
- pdo_sqlite driver installed

# Setup:
```bash
php -S localhost:8000
```